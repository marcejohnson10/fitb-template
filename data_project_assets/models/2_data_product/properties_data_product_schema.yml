##################################################################################################################################
# Following is an example of how to structure a schema yaml file. Each folder that contains models should also contain a 
# schema yaml file describing the models contained within the folder. 
##################################################################################################################################

version: 2

models:


    - name: org_dim_dp
#      description: "orders data product table"
      columns:
          - name: cont_id
            description: "The primary key for this table"
          - name: current_org_flag
            description: "is the org current flag"
#              - dbt_constraints.unique_key
#              - unique
#              - not_null
#              - accepted_values
#              - relationships

#sources: 
#  fitb_tech_transformation: 
#    - name: raw  
#      database: "{{env_var('DBT_DATABASE')}}"
#      schema: raw 