##################################################################################################################################
# Following is an example of how to structure a schema yaml file. Each folder that contains models should also contain a 
# schema yaml file describing the models contained within the folder. 
##################################################################################################################################

version: 2

models:


    - name: PR_ORG_LEGALFULLNM_DP
#      description: "orders results table"
      columns:
          - name: CONT_ID
#            description: "The primary key for this table"
            tests:
#              - dbt_constraints.unique_key
#              - unique
              - not_null
#              - accepted_values
#              - relationships

#sources:
#  fitb_tech_transformation: 
    - name: raw 
      database: "{{env_var('DBT_ENVIRONMENT')}}"
      schema: raw 